@page
@model Frontend.Pages.CMunicipio.IndexModel
@{
    ViewData["Title"]="Index Municipio";
}

<h1>Listado de Municipios</h1>
    <p>
        <a asp-page="Create" class="btn btn-info">Crear Municipio</a>
    </p>

    <table class="table table-striped">
        <thead class="table-dark">
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var mun in Model.Municipios)
            {
                <tr>
                    <td>@mun.Id</td>
                    <td>@mun.Nombre</td>
                    <td>
                        <a asp-page="./Edit" class="btn btn-secondary" asp-route-id="@mun.Id">Editar</a>
                        <a asp-page="./Details" class="btn btn-warning" asp-route-id="@mun.Id">Detalles</a>
                        <a asp-page="./Delete" class="btn btn-danger" asp-route-id="@mun.Id">Eliminar</a>
                    </td>
                </tr>
            }
            
        </tbody>
    </table>
